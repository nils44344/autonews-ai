// Placeholder homepage — wiped 2026-06-08 for new business direction.
// Replace once the owner names the new product.

export const revalidate = 60;

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 text-center">
      <div className="font-mono text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500">
        // REBUILDING
      </div>
      <h1 className="mt-6 max-w-2xl text-3xl font-bold leading-tight text-white sm:text-4xl md:text-5xl">
        Something new is coming.
      </h1>
      <p className="mt-4 max-w-md text-sm text-slate-400">
        autonews-ai.live is between products.
      </p>
    </main>
  );
}
